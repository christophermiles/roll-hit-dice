export type DiceCount = number
export type DieType = number
export type Modifier = number

export interface HitPointResults {
    minimum: number
    weak: number
    average: number
    strong: number
    maximum: number
}